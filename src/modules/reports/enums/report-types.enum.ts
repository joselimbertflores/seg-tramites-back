export enum reportType {
  APPLICANT = 'applicant',
  SEARCH = 'search',
  UNIT = 'unit',
  SEGMENTS = 'segments',
  UNLINK = 'unlink',
  HISTORY = 'history',
  EFFICIENCY = 'efficiency',
  UNIT_FLOW = 'unit_flow',
}
